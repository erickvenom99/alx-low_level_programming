#include "main.h"

/**
* _puts_recursion - prints a string
*@s: takes string char
*
* Return: Always 0 (Success)
*/

void _puts_recursion(char *s)
{
	if (*s == '\0')
	return;
	else
	{
		_putchar(*s);
	}
		_puts_recursion(s + 1);
		_putchar('\n');
		
}
